踩过的坑：

问题：图片无法显示

------------------------------------------------------------------------------------------------------------------------
问题：c:set标签不起作用
解决方法：web.xml文件头配置xlmns:xsi,指定version:2.5

------------------------------------------------------------------------------------------------------------------------
引入Shiro的maven依赖
在web.xml中配置shiroFilter
在application-context中配置shiroFilter(参考shiro官网整合springmvc）
编写Realm

------------------------------------------------------------------------------------------------------------------------
缓存的类型：
Ehcache 基于jvm，速度快，效率高，但是缓存共享麻烦，集群分布式应用不方便
Redis 通过socket访问缓存服务，效率比Ehcache低，比数据库快很多，处理集群和分布式缓存方便，有成熟的方案

// TODO springmvc + easy-ui
// TODO springmvc + vue
// TODO emacs java开发
